---
galaxy_info:
  role_name: gitlab_runner_app
  author: repository
  description: Install and configure GitLab CE lifecycle tasks
  license: internal
  min_ansible_version: "2.14"

dependencies:
  - role: debian_mirrors
