---
galaxy_info:
  role_name: traefik_app
  author: repository
  description: Deploy Traefik with Docker Compose lifecycle tasks
  license: internal
  min_ansible_version: "2.14"

dependencies:
  - role: debian_mirrors
