---
galaxy_info:
  role_name: docker_host
  author: repository
  description: Install Docker Engine and Compose plugin on Debian virtual machines
  license: internal
  min_ansible_version: "2.14"

dependencies:
  - role: debian_mirrors
