---
galaxy_info:
  role_name: compose_app
  author: repository
  description: Common lifecycle tasks for Docker Compose applications
  license: internal
  min_ansible_version: "2.14"

dependencies:
  - role: debian_mirrors
