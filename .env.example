# Public example only. Put real values in a local secret manager or private overlay.
OPENBAO_ADDR=https://openbao.example.test
OPENBAO_ID_TOKEN=replace-with-a-short-lived-token
TRAEFIK_DOMAIN=example.test
ETCD_INITIAL_CLUSTER_TOKEN=example-etcd-cluster-token
